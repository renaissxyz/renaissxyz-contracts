// SPDX-License-Identifier: MIT

pragma solidity ^0.8.0;

import "./RenaissRegistryV3.sol";

/**
 *  @dev pointing to the latest version of {RenaissRegistry}.
 */
contract RenaissRegistry is RenaissRegistryV3 {
/**
 * Never add any variables to this contract.
 * It is just used as a proxy to point to the correct
 * version of {RenaissRegistry} without losing the generic
 * name.
 */
}
